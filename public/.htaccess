# React SPA routing support
RewriteEngine On
RewriteBase /

# Jika file atau folder tidak ada, arahkan ke index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
